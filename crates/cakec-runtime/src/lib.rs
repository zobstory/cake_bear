//! Runtime library linked into every compiled cakebear binary.
//!
//! Phase 1 will hold `console.log` intrinsics and program entry glue.
//! Later phases add: GC, M:N async/await scheduler, channels, and the
//! standard library.
